{
  "name": "rshb",
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "new-version": "lerna version --conventional-commits --yes --exact",
    "diff": "lerna diff"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
